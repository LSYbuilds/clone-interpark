info.txt

#주석이다
# info.txt파일은 git 커밋목록에서 제외

*.db
# 모든 .db 파일은 커밋목록에서 제외

src/
# src 폴더의 모든 내용들은 제외

src/*.js
# src 폴더에 .js 파일만 제외

src /*.log
# src 폴더에 .log 파일만 제외

src/**/*.log
# src 폴더에 및 안쪽 폴더에 있는 .log 파일 제외

assets/
# assets 폴더는 제외

